# QuantMind 待办全景（截至 2026-09-17 夜）

> 汇总口径：P6 细案勾选状态 + 统一交易栈 P0–P9 路线图 + 11 份设计文档验收清单逐项审计（2026-09-17，84 项全核）+ 记忆在案的挂账项 + 当晚新发现。
> 状态标记：🔴 阻塞/在途（有明确验收时点）｜🟡 已计划未开工｜⚪ 决策待拍板｜✅ 近日已清（仅列本轮关闭的）。

---

## 一、P6 实时轨：8 项待验收（明起开盘自然推进）

P6 共 19 项任务：11 项已收口 [x]，8 项**落地完成、待盘中/连续验收** [~]。全部卡在"要有真实盘中数据"上，桥源席上线（今晚）后第一次开盘即可批量推进：

| 任务 | 余下的验收 |
|---|---|
| T-P6-02 桥源席 | 全圈节拍（`hot_set.last_cycle_s`）/ 529 只落地率 / 覆盖率闸门放行实况；SDK 推送语义待与 SDK 方核对（证据包 `data/p6_tdx_evidence_20260917/`） |
| T-P6-04 L0.5 | 连续 5 交易日全量落盘 + 每日质检 PASS + 容量复测（桥源席已并入归档，tag=bridge） |
| T-P6-05 时延 | P95 实测（打点为 TdxAiData 采集口径；桥源席未接 `intel:latency`，按需加 `market_snapshot_bridge` 档） |
| T-P6-07 增量特征 | **override 白名单裁定**（空白名单=行情只做闸门不参与打分；mom_ret 族可用、vol_std/liq_* 禁覆盖，见细案证据） |
| T-P6-09 回放验收 | 真实交易日 diff=0（依赖 L0.5 有数据——今晚已解锁） |
| T-P6-10 资源治理 | 开盘全速压测存档（订阅+15s 推理并发）/ 过载注入演练 / 预算线校准（3s 已定档可行，生产维持 15s） |
| T-P6-11 情报总线 | 本体已真机验收；剩生产观察（哨兵/副驾驶已在消费） |
| T-P6-13 日内 regime | 开盘确认指数帧落地（桥源席已能供 sh000001/sh000300）+ 收盘终值与日频一致性核对 |

**P6 完成定义（细案 §一）的 5 项全局验收**同样押在开盘数据上：端到端 <2s P95；情报告警闭环真实生效；哨兵误报率报表；F2 撮合三指标上交易台；可回放复现 diff=0。

---

## 二、今晚新确认（数据正确性，优先级最高）

### 2.1 估值 TTM 源侧损坏仍在持续，且已传导到训练/实时特征源 ★
- **实证（2026-09-17 夜）**：`5_technical_derived/valuation/dt=20260914` 长江电力 `net_profit_ttm=64.3亿 / pe_ttm=108.96`（真实 ~360 亿 / PE ~20）；
  同名坏值已出现在 **`6_ml_datasets/features_daily`（pe_ttm=108.96）与 `l1_factors`（fun_pe=108.96、fun_ep=0.0092）**。
- **影响面**：生产实时模型 48 特征里 `fun_pe / fun_pb / fun_mv_rank（+fun_np_growth/fun_roe）` 为估值族——**明早实时推理会带着坏估值打分**；批量直读推理同理（09-08 起的分区）。
- **现状**：本地修复脚本 `backend/scripts/repair_valuation_ttm.py` 上次只修到 `20260911`；上游云端源回归未修（本地无法阻止复发）。
- **待办**：① 决定修复范围（valuation 09-12+，以及 features_daily/l1_factors 是否同修——目前脚本只覆盖 valuation 数据集，需扩展或上游修）；② 把修复做成例行（每日/每周质检）；③ 或向云端源提缺陷。

### 2.2 6_ml_datasets 与技术派生集两晚未更新（疑似同一根因）
- 实测：`features_daily / l1_factors / l2_factors / valuation` 最新分区均为 **dt=20260914**（写入 09-15 01:27），09-16/09-17 两晚无新分区；而 `daily_forward` K 线已到 09-16 → **A 股每日同步（00:55）部分数据集在跑、第 6 类与技术派生停更**。
- 待办：查 00:55 同步链在 09-16/09-17 两次运行的日志/失败点（入口：`quantdb_daily_sync.py` dataset 表 + Redis `quantmind:sync_schedule:A`）。

### 2.3 今晚已修/在途（供对账）
- ✅ 实时基线迁 QuantDB 直读（commit `3d5cdfae`）；✅ 桥源席并入 L0.5；✅ trade/api 已拾码；
- 🔴 **引擎重启守望**：等训练容器 `qm-train-...122847` 结束自动重启（07:20 CST 兜底），新基线才在引擎生效。

---

## 三、运营与协作事项（在途/需人工）

| 事项 | 说明 |
|---|---|
| 大 QMT 桥恢复 | Windows 侧重载 `BIGQMT_REDIS_DRYRUN.py`（09:52 起离线）；恢复后复核与桥源席的 `stale_after_s` 互动（建议 ≥150s） |
| orders 重复键 | trade 服务 `orders_client_order_id_key` 重复键自今早 09:54 起 1294 次，待排查（TDX 委托落库路径） |
| SDK 方核对 | TdxAiData 订阅语义（prime/额度/额外开启项）——证据包已备 |
| 磁盘水位 | 根分区 85%（137G 余）；L0.5 留存（桥源席 ≈11MB/日、未来 3s 订阅态 ≈367MB/日）+ 90 天保留需盯 |
| QM_RUN.py 入仓 | Windows QMT 桥启动入口定稿副本尚未入库 |
| 备源席回线复核 | QMT 桥回线后检查 source 翻转是否形成抖动（记录不改） |

---

## 四、后续阶段路线（统一交易栈 P5–P9，"之前计划过"的主体）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P5 产品化** | 前端重设计（新手路径/写策略/提要求）、图表规范落地、合规四件、默认安全、共享/用户分层压测 | 部分落地（合规四件 ✅、默认安全 ✅、图表大部分 ✅）；**未做**：共享层×10 压测、客服视图+一键诊断、单用户边际成本实测、统一外观走查、5 分钟新手可用性测试、K 线万根性能、暗/亮双主题走查 |
| **P6 实时轨** | 见 §一 | 收口在途，开盘验收 |
| **P7 L2+tick** | L0.5（已并到 P6 落地）、TickCore 选型（WonderTrader SEL / hftbacktest 候选）、F3 队列位置模型、执行算法 TWAP/POV、tick 回放 <1s、执行报告（IS/滑点） | 未开工；**前置决策：tick 数据源 = TDX 桥逐笔 vs 采买 L2** |
| **P8 LLM/Agent** | 新闻事件分级信号化（过评估门槛）、风控审查 agent、运营 agent、影子自动 | 中枢（新闻情报 T-P6-12/14）已落地；**LLM 信号 IC 评估、Agent 白名单边界、盘中使用选型**待定 |
| **P9 多市场** | Market Adapter 逐市场复刻（HK→US） | 大部分提前落地（HK/US 数据/训练/推理/市场分析/模拟券商已通）；余：每市场"推理→模拟"E2E 口径的正式验收 |

---

## 五、设计文档验收清单审计（84 项全核）——"真未实现"分组

> 11 份文档均为 2026-09-15 设计稿；`MVP实施计划_后端/前端` 只覆盖 P0–P4（全绿）。**P5/P6/P7 范围内的未开工项是"真未实现"聚集的最大来源**（下表已剔除前述 P5/P7 路线项）。

**持仓管理与执行控制**：仓位决策四方式（Kelly/风险平价只在回测侧，活盘仅 equal/score/custom）+ regime 联动；执行算法 v1（TWAP/紧急直发/追价）；持仓面板"退出线距离/信号趋势/事件标记"；退出规则 7 类只做 5 类（缺风控强制/事件退出/定期再平衡）。

**风险控制体系**（除 kill switch 外近乎全表未落地——**与 P6 实时轨属同族，建议合并规划**）：RiskGate 尚未覆盖全部 5 条下单路径（手动路径是第二个实现，sandbox/镜像/实时事件无统一闸）；fail-closed 杀进程实测；规则库 L0–L6 分层；四级状态机（CAUTION/RESTRICT/HALT）；全撤 cancel_all；规则影子模式+误杀率报表；决策记录配置版本号；拦截率/误杀率监控；停机演练报告。

**模型量化细则**：Model Contract 三入口强校验（`inference_contract.py` 是死代码、全仓零引用）；未来扰动/复权一致性/PIT 抽检进 CI；标签规范逐模型审计回填；分层组合收益+衰减半衰期进模型卡；滚动 IC/PSI/预测分布/分层四类监控接交易台（`model_ic_monitor.py` 未入调度）；**特征注册表 + list_hash 强校验**。

**可维护性与静默失效防治**：Runbook top-20（无）；**体检 12 项进每日任务**（脚本+健康卡 ✅，调度未接）；12 条回归进 CI（无 CI 载体）；ADR 首批 10 条（无目录）；定位 ≤10 文件度量复核；DR 演练（PG 恢复→对账→交易恢复）；RBAC 五角色+管理员双人复核（现只有 admin 单角色）。

**评估与打分体系**：分数联动生效（选股 D 级跳过/策略 C 级降仓/模型降权——全仓无 grade→动作链路）；评分可复现 CI 断言；动线四任务 ≤3 步验收。（五卡/体检九项/证据矩阵等主体 ✅ 已实现。）

**行情扫描与机会发现**：v1 扫描器集只落了 `model_signal` 一路（形态/资金流/事件未做）；批扫描性能实测；交易台机会面板（合并/冷却规则已有，前端无面板）；扫描器评分卡回填；交易→机会来源回溯（订单无 opportunity_id 链路）。

**机构级标准与粒度对齐**：五规则进 CI；SLO 六项进看板；变更管理/回滚/演练制度（首次记录）。

**Strategy Lab**：1000 万行 csi300 5 年回测 60s 性能实测（唯一真未实现的硬指标；其余多为 live LLM 跑分/重跑实证类"部分"）。

**图表与可视化**：K 线万根性能；暗/亮双主题走查。（主体 ✅。）

**前端界面设计**：统一外观走查；新手 5 分钟可用性测试。（其余 ✅/部分。）

---

## 六、挂账小项（记忆在案）

- 估值修复后 **PG 侧回填**（现库中未见同名 `features_daily` 表，需核实该项是否已随直读迁移失效）；
- HK 训练页**市场/数据源选择器**（现仅 A 股导向）；HK 新因子 size/流动性残差中性复检（研究项）；
- 融合模型 metrics 占位（val_ic=0），样本外 IC 评估后续可加；
- `signal_side` 阈值仍硬编码默认（未做配置面）；
- minibt 结果目录 `db/ai_ide_tmp/{user}/{job_id}_result` 累积清理（后续项）；
- **远端节点包（免 Docker）**：规划定稿待开工——remote orchestrator process 分支 / node_manager 探测 / build_node_pack.sh / 31.66 端到端（4 步）；
- 便携包 **Windows 真机验证**：start.bat 的 pack.env 解析、MODELS_PRODUCTION/USER_MODELS_ROOT 等变量；Mac 包理论可行未做；
- 通用设置页/新设置中心（模式切换等挂点；`SettingsPage.tsx` 是死文件）；
- 期货夜盘"品种级"时段（会话级已支持）、策略级跟踪误差（快照无 strategy 维度）。

---

## 七、待拍板决策（⚪ 汇总）

1. **哨兵误报率口径与达标线**（P6 §六.4，建议 ≤30% 先跑数再收紧）；
2. **tick 数据源**（TDX 桥逐笔 vs 采买 L2——决定 P7 启动时点）；
3. 新闻 veto 默认强度（直接否决 vs 仅降仓 vs 仅告警——现为标记+买侧 veto）；
4. LLM/Agent：盘中使用选型（本地小模型 vs API）、TradingAgents 信号化口径、写操作白名单边界（建议初期全审批）；
5. 晋级实盘硬门槛取值（模拟天数/跟踪误差/最大回撤）；
6. QMT 虚拟成交价来源确认（实时盘口 vs EOD）；
7. 实时推理 override 白名单裁定（决定行情是否进入打分特征）；
8. 估值修复范围（valuation 单集 vs 连同 features_daily/l1_factors）。

---

## 八、建议优先序（未来 3 天）

1. **明早开盘**：盯 P6 验收批量推进（桥源席→覆盖率→实时推理发布→L0.5 落盘→regime 指数）；引擎守望确认重启；
2. **数据链**：查 6_ml_datasets 停更根因（§2.2）→ 决定估值修复范围（§2.1）→ 补跑修复；
3. **P6 收尾**：override 白名单裁定（§七.7）→ 哨兵误报口径（§七.1）→ T-P6-10 压测存档；
4. **下一阶段启动项**：风险控制体系（与 P6 同族、且是真未实现最大块）+ P7 tick 数据源决策。
