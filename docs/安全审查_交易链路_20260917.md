# 交易链路安全审查（2026-09-17）与处置台账

> 方法：只读代码审查（路由鉴权全量 AST 枚举 + 注入/密钥/沙箱面人工核）＋当夜修复与实测验证。
> 范围：`backend/services/{trade,live_trading,simulation}`、`shared/`、`api/`、`engine/`（含 strategy_lab/ai_ide）、compose/nginx。

## 一、今夜已修复（含实测证据）

| 编号 | 问题 | 修复 | 验证证据 |
|---|---|---|---|
| **C1** | 内部信任链公开默认密钥（`changeme-internal-secret`）＋无头剥离：匿名者凭两枚请求头冒充任意用户（含 admin）并经 `/internal/strategy/order` 直下真单（缺省 REAL） | ① `shared/auth.get_internal_call_secret` 重构：**runtime.env 权威**（热换免重启）＋公开默认值一律视为未配置＋空=必败；② 全部校验点改实时读取（trade `verify_internal_call` 去导入期快照、admin 训练回执、engine 中间件已有守卫）；③ 发送端单 helper 已覆盖（orchestrators/preflight 改 helper）；④ **nginx + trade_proxy 双层剥离** `X-Internal-Call/X-User-Id/X-Tenant-Id`；⑤ `main_oss` 启动守卫改为"缺失即生成强随机并落 runtime.env"；⑥ **已轮换**（runtime.env + host .env 同步，sha8 d87334be） | 引擎 8001：新密钥 200 / 旧默认 401；trade `/internal/strategy/*`：新密钥过闸（500/422 为业务层）、旧 401；sync-trade：无密钥 401 / 有 422；匿名全 401；4 服务健康 |
| **H1** | `POST /api/v1/internal/sync-trade` 无鉴权 → 匿名可伪造任意组合成交/篡改资金持仓 | 服务端加 `verify_internal_call`；调用端（`remote_service`）补内部头 | 无密钥 401 / 有密钥过鉴权（422） |
| **H2** | `POST /api/v1/simulation/batch/step` 无鉴权 → 代任意用户触发日终结结算 | 升 `require_admin`（全仓无调用方） | 依赖注入生效（模块导入冒烟） |
| **H4** | `POST /tdx/config` 仅要求登录 → 任意用户可改全局桥地址/token（SSRF+凭证外带+真单通道劫持） | 升 `require_admin`（全局配置=管理员面） | 同上 |
| **M1** | 失败日志记录内部密钥原文 | 只记尝试值 sha256 前 8 位 | 代码 + 单测 |
| **M2** | `.env.bak-*` 备份未 gitignore、`.env` 644 | `.gitignore` 加 `.env.*`（check-ignore 命中）；备份 chmod 600 | `git check-ignore -v` 命中 |

**回归测试**：`backend/tests/test_internal_call_secret.py` 4/4（文件权威/公开默认拒绝/空 fail-closed/校验函数三态）。

## 二、在册未修（T-SEC 批次，建议下一批按序做）

| 编号 | 问题 | 要点 | 建议 |
|---|---|---|---|
| **C2** | 回放 `mode=code` 在 API/engine 进程内 `exec()` 用户代码；AST 黑名单可被 `getattr(obj,"__cla"+"ss__")` 字符串拼接绕过 → 容器内 RCE（直接持有 DB/Redis 凭据） | `simulation/replay/code_runner.py`；受限 builtins 里保留了 `getattr/setattr/type/object` 逃逸原语 | ① 移除逃逸原语（白名单收紧）；② 执行改**子进程**（复用 strategy_lab subprocess_runner 模式）或容器；③ 补负例测试（动态 dunder 拼接必须拒） |
| **H3** | 券商凭据明文 Redis（`broker:config:*` / `trade:tdx_config:runtime`）；`shared/encryption.py`（Fernet）未接线；`QUANTMIND_MASTER_KEY` 缺省硬编码 | 拿到 Redis 即接管真实交易通道 | ① 敏感字段接线 Fernet（主密钥入 runtime.env，轮换流程同 C1）；② 一次性迁移存量配置；③ 兼容读旧明文（迁移期） |
| **H5** | 用户代码沙箱无 OS 级隔离：ai_ide 容器无 cap_drop/非 root/只读/独立网络；只读 DB 凭据失败**静默回落主库凭据**；strategy_lab 子进程仅 env 过滤 | 用户代码可直连内网 PG/Redis | ① ai_ide 容器加 `cap_drop=ALL`/非 root/独立网络/只读根；② 凭据回落改**失败即拒**；③ strategy_lab 加 cgroup/网络隔离（Day-7 项） |
| **M3** | 回放模型目录 `model_id` 未拒绝 `..`（路径穿越/DuckDB 拼接） | `replay/router.py:291`、`signal_generator.py:40/78` | `model_id` 白名单正则（`^[A-Za-z0-9_.-]+$` 且无 `..`）+ 路径 resolve 后必须仍在 base 内 |

## 三、已做得好的防线（保持）

- 真单镜像全链路 fail-closed（双开关/kill switch/白名单空=不镜像/Lua 原子限额/20% sanity/连续拒绝自熔断）；
- 真实交易默认关闭在**服务端**（`ENABLE_REAL_TRADING=false` 时 REAL 一律回落 Paper）；
- `broker_config` 全端点 require_admin + 改 host 必须重输密钥 + 审计只记字段名；
- 身份归一化拒绝伪造（请求体 user_id 与鉴权上下文不符 403）；
- QMT agent 凭据 bcrypt / 桥会话 token sha256；
- SQL 标识符白名单两处；命令注入全仓零命中；日志密钥插值逐点核过（仅 M1 一处，已修）。

## 四、核查无发现（含方法）

命令注入（全仓 `shell=True/os.system` 零命中）；交易主链 SQL 注入（全部拼接点逐一手审，仅 M3 越界）；SSRF（除 H4 用户可控 bridge_url 外全为配置固定地址）；engine/tasks 用户代码执行（0 命中；执行入口全集= C2/H5 三处）；`.env` 提交历史（干净）。

---

**注**：C1 涉及的旧密钥（旧 `INTERNAL_CALL_SECRET` 公开默认值、历史 broker 配置内凭据、`TDX_BRIDGE_TOKEN`）按"视同已泄露"处理——C1 已轮换；H3 落地时同步轮换 broker/TDX 凭据。
