# P6 实时轨实施细案（盘中快照级：情报总线 · 热集推理 · 哨兵推送 · F2 撮合）

> 版本：v1.0（2026-09-16）　承接：`docs/统一交易栈_设计方案.md` §3.2/§六/§八、`docs/L2数据与实时推理_工程方案.md`（P7 tick 层）、`docs/风险控制体系_设计方案.md`、`docs/评估与打分体系_设计方案.md`
> 定位：MVP（P0–P4）已全绿 + gitee/master 合流后的**下一阶段主体**。本文把设计文档的 P6 蓝图落成可勾选的 T-P6-xx 任务。
> 使用方式：每个任务是完成态最小单元——`代码 + 测试 + 验收全过` 才可勾选 `[x]`；提交信息引用 ID（如 `feat(T-P6-01): ...`）。
> 状态图例：`[ ]` 未开始　`[~]` 进行中　`[x]` 完成　`[-]` 推迟（须写原因）
> **质量基线与 MVP 一致并加严**：机构级 = 单元（金样）+ 集成（真库/真 Redis E2E：造数→断言→清理）+ 降级/边界路径显式 + 源守卫（单实现）+ 性能基准（延迟预算逐环节实测存档）+ 相关全量回归「集合差零新增失败」+ 前端 typecheck/组件测试。

---

## 一、目标与验收（P6 完成定义）

**一句话**：从「T+1 日频」升级到「盘中快照级闭环」——行情快照 3s 级进入系统，热集（持仓+候选+异动）
增量推理出信号，情报总线统一消费，哨兵把「值得注意的事」推给人，模拟撮合升级到 F2（盘口约束）。

| 验收项 | 判据（可测） | 度量方式 |
|---|---|---|
| 端到端延迟 | 行情到达 → 信号/告警可消费 < 2s（P95，含热集推理） | `intel:latency` 滚动统计 + 交易台面板（T-P6-05/16） |
| 情报告警闭环 | 新闻风险事件/市场 regime/异动 → 总线事件 → 风控 veto 或策略过滤 **真实生效** | 拦截计数（复用 mirror:skipped / risk_lock 审计先例）+ 事件回看（T-P6-12~14） |
| 哨兵误报率 | 告警留痕 → 事后标注（T+1 命中回填 + 人工标注）→ 误报率报表 | 报表页 + 周报口径（T-P6-15，口径见 §六.4） |
| F2 撮合 | 模拟成交价来自快照盘口（含涨跌停排队/深度约束/部分成交） | 成交价偏差分布/成交率/部分成交比例三指标上交易台（T-P6-17/18） |
| 可回放复现 | 实时信号可用留存快照回放复现（同数据/同特征/同模型 → 同信号 diff=0） | 验收器脚本 + 回归用例（T-P6-09） |

**明确不做（P6 边界，防范围蔓延）**：
- tick/逐笔级撮合与队列建模（F3 → P7，依赖 L2 数据落地）
- 执行算法 TWAP/POV（P7）
- 毫秒级策略（诚实边界：快照级策略 = 分钟级可靠、秒级边缘）
- 多市场实时轨（P6 先 A 股；HK/US 沿用日频；行情适配层预留市场参数）

---

## 二、总体架构（设计文档 §3.2 的落地形态）

```
盘中（快照 3s 级）
行情快照流 ──→ 实时特征（增量） ──→ 热集推理 ──→ 实时信号 ─┐
新闻/RSS 流 ─→ 情绪打分/实体链接 ─→ 事件分级 ──────────────┼→ 盘中情报总线 ─→ ①风控闸门（veto/降仓）
指数/涨跌停/成交额 ─→ 市场状态（regime） ──────────────────┤   （统一事件流）   ②策略（事件驱动/过滤）
量价异动/账户异常 ─→ 识别引擎 ─────────────────────────────┘                 ③哨兵推送 → 副驾驶建议
```

三条纪律（沿用主干设计）：
1. **同契约不同源**：实时信号仍写 Signal 契约（`source=realtime` + 快照精度时间戳），策略无感切换批量/实时。
2. **单实现**：特征定义、撮合规则、风控闸门全部复用既有唯一实现，实时只做「增量调度」不做第二套口径。
3. **可复现**：实时链路任何产出都必须能用留存快照回放复现。

---

## 三、依赖与前置（防踩空）

| 依赖 | 状态 | 说明 |
|---|---|---|
| P1–P4（契约/执行统一/策略/评估） | ✅ | 实时轨挂既有契约与唯一实现 |
| gitee/master 合流 | ✅ 2026-09-16（c638d44b） | 上游执行安全（真实时段校验/流动性约束）已在位 |
| **行情快照源** | ✅ **已落地（T-P6-01，2026-09-16）** | **TdxAiData**（`/opt/tdx-aidata`，免 Windows；订阅推送=实时主源[五档全字段/零请求配额]，请求接口=3 次/窗口低频补充；桥降为备用）；前端可配 + 自检上线 |
| L0.5 快照留存（3s 快照按日落盘） | ⏳ 见 §4.4 | P6 只做「热集切片」，不依赖 10GB/日 tick 全量（P7） |
| 实时信号流开关 | ⚠️ 现状默认关 | `ENABLE_SIGNAL_STREAM_PUBLISH=false`；T-P6-08 需开启并压测 |
| ONNX 导出链 | ⏳ 空白 | loader 有预留（`inference/model_loader.py`），无导出器；T-P6-08 前置 |

---

## 四、现状资产盘点与缺口（2026-09-16 三路代码核查）

### 4.1 行情快照链路

**已可复用**：
- **键规范已统一**：`market:snapshot:{sh600036}`（Hash，TTL 300s）+ `market:series:{SH600036}`（ZSET，
  score=ts，成员 JSON 含 price/is_stale/source，保留 6000 点）——stream 读、TDX Feed 写、模拟撮合/
  实盘预检/账户重估读，口径一致（`AGENTS.md` 键规范）。
- **TDX 桥 3s 快照能力（含五档）**：桥侧 `get_market_snapshot` 自带 3s 内存缓存 + 5 档
  Buyv/Sellv + Inside/Outside，限流 60 次/分——**作为增强源**（TdxAiData 为默认源）。
- **3s 采样与留存骨架已跑**：`live_trading/services/tdx_quote_feed.py`（POLL 3s、只覆盖持仓股，
  单只实际刷新 `max(3, min(30, 持仓数×1.5))s`）→ Redis + PG `tdx_position_ticks/sessions`
  （已建表、暂无消费方——P6 回放的现成原料）。
- **TdxAiData 通道就绪（本机）**：`/opt/tdx-aidata`（libTdxAiData.so + TdxAiData.ini + tqServer.py），
  客户端参考实现 274 行（`get_klines / get_quote / get_minute_data / get_tick_data / subscribe`），
  分时/分笔/订阅齐全，**Linux 原生、免 Windows**；已验证与桥逐字段对拍一致（close/volume/amount）。
  已知坑：`count` 参数模式不可用（用 start/end 区间）；突发 `Token Insufficient`（码 13）需指数退避。
- **取价契约单实现**：`execution_engine.py::_resolve_fill_price` 四级新鲜度 + strict 守卫；合流后
  已带**流动性约束**（`_reserve_liquidity`：参与率 10% 默认、部分成交与余量排队）。
- **观测三件套**：`/tdx/quote-feed/status`、桥健康预检、时序新鲜度预检。
- **新闻情绪闭环（分钟级）**：Huntly + RSSHub → enricher（词典 0.6 + FinBERT 0.4）→ PG
  `news_article_enrichment`，celery beat 每 60s 富化。
- **stream 推送骨架**：WS `/ws` + 四条 pusher（quote/trade/strategy/notification），2s 批量分发。

**缺口（P6 要补）**：
1. **无全市场/热集快照**：`market:snapshot/series` 实质只有持仓股（单用户）+ 公共免费行情服；
   热集外标的全部回落 T-1 日线——第一前置缺口（T-P6-01/02 解决）。
2. **stream 无 3s 快照生成器 / 无分钟 K 线**：QuantDB 分钟级返回空，只能用快照合成"当前 K 线"。
3. **盘口深度未进链路**：`map_snapshot` 丢弃五档；唯一落地是 L2 采集 60s 级 PG 表。
4. **WS topic 鉴权缺口（安全）**：仅 `notification.*` 有归属校验，`stock.*/strategy.*/trade.updates.*`
   无校验——情报 topic 接入前必须补（T-P6-11 前置项）。
5. **无情报总线、无实时市场宽度**（T-P6-11/13 解决）；~~无端到端时延度量~~ → **已落地**
   （T-P6-05，2026-09-17：`intel:latency` 滚动统计 + `latency_report` 报表；总线消费点待 T-P6-11 接入）。

### 4.2 推理与信号链路

**已可复用**：
- **批量推理链**：celery beat 08:00 `auto_inference_if_needed`（扫描 running 组合 + 设置 + 系统兜底）/
  管理端 `POST /admin/models/run-inference` / 用户态 `/inference/run|batch`；执行器
  `InferenceScriptRunner`（子进程跑模型目录 `inference.py`，parquet 模板自动刷新）。
- **Signal 契约四列已就位**：`market/rank_pct/source/signal_ts` 自愈 DDL；`source=batch|realtime`
  常量已定义（`signal_contract.py`）；**`POST /engine/runs/{run_id}/signal-ready` 端点已存在且零调用方**
  ——实时信号写入面现成（rank_pct 自动算）。
- **就绪标记**：`qm:inference:completed:{date}`、`qm:signal:ready:{market}:{date}`（全量校验通过才置位）、
  `qm:signal:latest:{tenant}:{user}`；单实例锁 `inference_lock.py` 可复用。
- **消费面**：SignalLoader（含 `load_signals_by_symbols` 按需小集合读）；BUY/SELL 闸门
  `_resolve_signal_sides`（分位 20% + 稳健归一 ±0.2 + consensus≥4 + confidence≥0.3）；
  实盘 runner 消费 `qm:signal:stream`（开关默认关）。
- **热集候选雏形**：`_resolve_watchlist`（当日推理 TopN + 全持仓去重，上限 50，**硬编码单用户**）
  ——泛化为多用户即热集服务起点。
- **模型注册表** + 逐 run `duration_ms` 留档；`backfill_inference_quality` 每日 02:30 回填。
- **实测基线**：全市场 A 股单日批量推理端到端 **≈20s**（2026-09-16 20:23 实测）。

**缺口**：
1. **模型级热集推理入口不存在**：模板只吃整日全市场 parquet，`symbols=` 仅后置裁剪；
   需"按符号集喂特征 → 批量出分"的常驻/增量推理服务。
2. **ONNX 导出链空白**（<100ms 级推理依赖）；现有 `InferenceService.predict_batch` 特征口径与
   parquet-quantdb 主链路不一致，不可直接复用。
3. **"全用户持仓并集"不存在**（L2 采集硬编码 default/00000001）。
4. ~~**实时特征引擎不存在**：现有 `tdx_l2_realtime` 是 13 因子规则打分（默认关），非"复用训练因子定义的增量特征"。~~
   → **已解决（T-P6-07，2026-09-17）**：`shared/feature_incremental.py`（32 列快车道，与批量
   feature_defs 逐值金样锁定）+ `inference/incremental_features.py`（快照状态机 + provenance）。
   列口径裁定表（快照 vendor 列 vs 我们的定义）待 T-P6-08 接线前完成。
5. **哨兵误报率无口径**：无告警标注表/回填；仅计数型埋点（T-P6-15 补）。
6. **IC 监控无调度**（CLI 手动）——P6 将其接入"模型异常"识别（T-P6-14）。
7. **信号流发布默认关**、无压测。

### 4.3 通知 / 风控 / 执行链路

**已可复用**：
- **通知管道全链**：`notification_publisher`（PG `notifications` + Redis Stream `notification_events`
  → `NotificationPusher` → WS `notification.{user_id}` → 前端 `useNotifications`，鉴权/去重/断线降级轮询
  齐全）+ 类型/级别白名单 + action_url→路由映射 + 埋点计数器（`notification_metrics`）。
- **告警生产者盘点（16 处）**：订单拒/成交/超时、sltp 执行器、风控触发 scanner（60s）、镜像熔断、
  双轨对账、收盘核对、体检退化、回测完成、推理兜底、数据质量（**链路已损坏**：列名/类型错+被吞，
  见缺口）——哨兵=在这些之上做"分级+留痕+误报闭环"，不新建推送骨架。
- **风控闸门链**（下单路径）：SIM 链（池过滤→退出→调仓→risk_lock 买锁→改量→撮合）；REAL 链
  （整手预检→check_order_risk→提交）；镜像链（开关/白名单/偏离 ±2%/资金持仓/Lua 原子限额/熔断）；
  desk 三重闸门（SIMULATION 限定/60s NX 防重 fail-closed/退出单不可改）。**veto 挂点现成**。
- **扫描器范式**：`risk_trigger_scanner`（60s + 交易时段门 + Redis 心跳 + 异常隔离）与
  `strategy_monitor_pusher`（指纹去抖）——哨兵 worker 照此模式。
- **副驾驶积木**：QuantBot 路由（chat SSE + 因子能力）+ `RichContentRenderer` 卡片 + desk
  `PlanCard/DeskCards`（含下钻与改量交互）。
- **一键链**：`OrderRouter.submit_order` 唯一入口（幂等/落账/镜像）。

**缺口**：
1. **无总线本体**：四条独立 Stream 各自为政，无统一事件模型/分级/消费 SDK/投递点。
2. **市场异动规则引擎是死代码**：`create_anomaly` 无调用方；`qm_market_anomalies` 只读不写。
3. **`data_quality` 通知链路静默损坏**（`gen_random_uuid` 插 SERIAL id、列名 `type`≠`notification_type`，
   被吞成 warning）——修复并入 T-P6-11。
4. **告警无结构化字段**（notifications 无 symbol/severity，前端靠文本正则分市场）；**前端
   `IntelligenceAlertsCard` 是写死 mock**。
5. **QuantBot 无实时上下文**（不读持仓/异动/告警状态）；无"操作建议卡"（accept/reject/一键执行）。
6. **WS topic 鉴权缺口**（同 §4.1-4）。

### 4.4 数据留存与磁盘

**已有**：
- PG 热集留存已跑 3 周：`tdx_l2_snapshot`（60s 级、183 只、**含 bid5/ask5**、487k 行）、
  `tdx_position_ticks`（3s 级、71 只、118k 行，现网持续写）、`tdx_l2_daily`。
- 离线 L2 仓库（NAS，独立项目）：归一化 parquet 逐笔/十档/分钟聚合至 20260911（seagate 1.8T +
  sata 959G）——**定位=离线批量回放/特征批算**（CIFS 挂载，不得作实时链路存储）。
- QuantDB 日线/指数（`index_daily` 2602 分区至 20260914）；`data/market-analysis/` 每日快照归档。
- Wind tick/l2 单双日样本（格式对齐基线）。

**缺口**：
1. ~~**无"按日落盘"分区留存**（PG 单表 ≠ 留存层；升到全市场即不可行）；无 schema 版本/降冷/质检。~~
   → **已解决（T-P6-04，2026-09-17）**：`data/l05_snapshots/date=YYYYMMDD/` 按日落盘（订阅写侧内嵌
   归档）+ 日质检 + 自动降冷（keep_days=90）；实测 145 B/行、3s×527 只 ≈ 367 MB/交易日。
2. **min1 分钟线停更**（最后 2026-08-25，无日常写入者）；l2-warehouse `agg/minute` 无消费路径。
3. **留存治理（PG 三表）仍为零**：三张 PG 表无清理任务；根分区 84% 已用——L0.5 侧已有容量标定与
   降冷（T-P6-04），PG 侧待后续清理任务。
4. `l2-sata-fuse` 容器为占位（sleep infinity、无挂载）——**细案不依赖它**。

---

## 五、任务分批（T-P6-xx）

> 每任务 = 代码 + 测试（机构级，见文首基线）+ 验收全过才可勾。测试类型缩写：
> U=单元/金样　I=集成（真库/真Redis E2E）　P=性能基准（实测存档）　D=降级/边界　G=源守卫　R=相关回归。

### 批次一：行情源与 L0.5 留存（第一前置，先做）

- [x] **T-P6-01 TdxAiData 行情接入（免 Windows 主源）**（2026-09-16 深夜落地）
  **落地**：`backend/shared/tdx_aidata/` 四模块——`config`（Redis 配置 > env > 默认；Token 外科手术写
  `TdxAiData.ini`，仅回掩码）／`protocol`（周期映射、count→区间换算、K 线 NaN 归一、**订阅推送解析器**
  （真机样本固化））／`worker`（**唯一 SDK 引入口**：独立子进程 chdir 隔离 + 崩溃隔离 + **配额闸门**
  [每窗口 3 次实测、冷却 180s 起翻倍封顶 600s、超额快速失败不触 SDK] + fd 级诊断捕获）／`client`
  （按需拉起[flock 防竞拉]/超时/错误映射/重启）。API 四端点（config GET+POST / selfcheck / restart）+
  前端「数据源设置」面板（目录/Token/开关/自检/配额窗口可视化）+ docker-compose 挂载 `/opt/tdx-aidata`。
  **实测（决定性，改写采集架构）**：① 请求接口**每冷却窗口仅 3 次**（全接口共享，第 4 次 Token
  Insufficient；订阅期间请求仍受限流）→ 请求=低频补充（K线/分时/分笔）；② **`subscribe()` 订阅通道
  可建立且零请求配额**——真机 180s 收 54 帧（晚间每 ~10s/批×3 标的，盘中节拍待开盘复测），
  载荷 39 列全景：**price/pre_close/open/high/low/limit_up/limit_down/cage_up/down/tomorrow_limit_*/
  seal_amount(封单)** + **ask1-5/bid1-5 五档十档全量** → **订阅推送=盘中实时主源**（T-P6-02 据此
  重构：不再"轮询请求"而是"订阅+推送落 Redis"）；③ TDX 桥降为**备用/对照增强**（不再必需）。
  **验收证据**：真机 E2E 全绿（拉起→ping→真实日K→真实快照；限流窗口下如实 skip 携带 retry_after）；
  自检端点上线（openapi 验证）；20/20 测试（config 8 + channel 10 + live 2）；前端 typecheck 零错。
  **测试**：U（配置优先级/ini 手术/解析器真样本/错误映射）＋ I（真机全链，限流感知）＋ D（预算闸门
  快速失败/目录缺失/未配 Token/worker 猝死恢复）＋ G（tqServer 唯一引入口源守卫）＋ R（相关回归）。
  **部署注记**：compose 挂载于下次容器重建生效（当前容器已以同内容副本验证全链）。

- [~] **T-P6-02 热集快照采集：订阅驱动 → Redis 标准键（T-P6-01 实测定案）**（2026-09-16 夜首落；
  2026-09-17 凌晨修复 100 只订阅上限静默失效 → 分片集群上线，盘中验收待开盘）
  **落地（部分）**：`collector.py`（帧→标准键映射[消费方契约字段 Now/Open/PreClose + 五档全景]、
  热集差分[上限截断]、静默重订阅、`SubscriptionEngine` 常驻循环[队列→Redis 批量写]）；
  worker 订阅模式挂载（env/Redis 配置开关；`subscription_status`/`hot_set_sync` IPC）；
  消费方零改动：写 `market:snapshot`(小写前缀 Hash) + `market:series`(大写前缀 ZSET)——与
  stream/预检/撮合读取完全同构。
  **夜测实况**：真机 E2E——热集播种 → worker 拉起 → 订阅建立（resubscribes=1）→ 数据帧到达
  （records=4 → written=4）→ Redis 逐值对拍通过（Now=40.92/PreClose=41.1/bid1=40.91 与推送一致）；
  热集变更→重订阅差分测试通过。**帧型分类**：`ColDes` 全列 + `Content` 空行 = 元帧（ACK/心跳，
  深夜市场关后仅剩此类），数据帧带行——采集器分计数（frames_data/frames_meta），测试只认数据帧
  （深夜观察窗仅元帧时如实 skip 携带证据）。
  **⚠ 事故与修复（2026-09-17 凌晨深挖，决定性）**：生产 worker 每次启动打印一次
  「[错误码 2] 股票代码错误」——SDK `_check_result` **只打印不抛**，引擎侧 subscribed=527 系
  本地记账，**整批订阅实际从未生效（零帧到达）**——典型静默失效。二分实测（独立测试键+独立
  socket 逐尺寸探针，回读 frames_meta 活体计数）：**单次 subscribe ≤100 只正常（100 只≈200
  元帧/30s）；101 只起整批拒绝（错误码 2、零帧）**；原生两次 subscribe 为**替换语义**（非累加）
  ——单进程无法承载 >100。
  **修复=分片集群**：`shard_symbols`（crc32%N 确定性切分，热集增删不重排）+ 引擎
  `shard_id/shard_count`（超限硬截断 + `over_cap` 显式计数，绝不静默）+ `TdxAiDataCluster`
  （client 层：N 片各独立进程/socket[.s{i}]/SDK 连接，写同一标准键天然合并；状态聚合含逐片明细
  + shards_up；任一静默即 `silent` 置位并 `silent_shards` 点名）；分片数取配置
  （Redis `qm:market:tdx_aidata:config.shard_count` > env QM_SUB_SHARDS > 1，经验值
  ceil(热集/100)，保存后 restart 生效）；l05 归档文件名带分片 tag（`part-<ms>-s{i}-<seq>`）
  防多 worker 同毫秒撞车；前端面板 degraded 态 + 分片 x/y 展示。
  **生产验证（2026-09-17 凌晨）**：527 只切 **6 片**（85/96/83/96/80/87），6/6 片 SDK 就绪、
  订阅全建、**每片元帧流动（240–510/片，夜盘活体证明）**——修复前开盘将零帧。真机 E2E
  （2 片 × 120 只）16.7s 通过；测试 54/54（分片纯函数完备/不交/确定/均衡、引擎超限截断、
  空片不订阅、聚合口径、l05 文件名防撞、真机 2 片 E2E）。
  **待办（盘中验收）**：① 开盘复测数据帧节拍（P95 帧间隔）与 527 只落地率（分片后全覆盖）；
  ④ worker 复订阅仍为「按需拉起」（首次 admin 状态/自检触发）——容器重启后需一次触发才重建
  订阅；常驻监督（自愈/心跳）接线随 T-P6-10 一并做；
  ② ~~热集服务接线后切默认开~~ → 已接线（T-P6-06 落地当晚）；~~生产 worker 527 只持续订阅运行~~
  → **该表述已被上节事故证伪并修复**（分片集群上线后为真）；③ ~~L0.5 落盘（T-P6-04）消费同一写侧~~
  → **已落地**：T-P6-04 归档器内嵌本写侧（2026-09-17，Redis 写与归档两路独立）。
  目标：worker 增订阅模式——按热集符号集 `subscribe()`，推送帧经 `protocol.parse_push_payload`
  解析后写入**标准键** `market:snapshot`（Hash，含五档）+ `market:series`（ZSET），消费方
  （stream/预检/撮合/账户重估）零改动即得实时数据；热集变更→增量 subscribe/unsubscribe；
  断连重订阅（sdk 回调静默失败检测：按最后帧时间心跳）。请求接口仅用于每日收盘后补数（≤3 次/窗口
  的低频用法：指数快照/关键标的日K校验）。桥保留为备用源（AiData 断流时降级，status 如实标注）。
  验收：**盘中实测**订阅帧节拍（P95 帧间隔记录，晚间实测 ~10s/批为下限参照）与热集 300 只全
  落地标准键；断流/限流降级显式；Redis 键与 TG 快照字段逐值对拍。
  测试：U（帧→键映射/热集增量订阅差分/心跳判定）＋ I（真机订阅 2-3 只 → 真 Redis 逐值断言 →
  清理；真机限流时如实 skip）＋ D（断流重订阅/超限截断/闭市空帧）＋ G（标准键唯一写入面）＋ R（既有消费方回归）。

- [x] **T-P6-03 快照结构扩展：五档 + 全字段（向后兼容）**（2026-09-17 收口：**被 T-P6-02 吸收**）
  **裁定**：五档（bid1-5/ask1-5 价量全 20 字段）+ 涨跌停/封单等全字段已在 T-P6-02 帧→标准键
  映射落地（`market:snapshot` Hash 五档全景），真机 E2E 逐值对拍通过；**Inside/Outside（内外盘）
  在 TDX 推送载荷（39 列）中不存在**——不在本任务范围，如需内外盘口径转 P7/总线阶段用逐笔
  数据衍生评估。旧消费方零破坏由既有回归承担（stream 快照源/预检/撮合/账户重估测试族全绿）。
  **收口动作**：真机订阅测试断言强化为**全 20 笔档字段**逐一校验（2026-09-17）。

- [~] **T-P6-04 L0.5 热集快照按日落盘（parquet 分区 + 质检 + 降冷）**（2026-09-17 凌晨落地，开盘起累计 5 日验收）
  **落地**：`shared/l05_store.py`——① 落盘 `data/l05_snapshots/date=YYYYMMDD/part-*.parquet`
  （zstd；行按 (symbol,ts) 排序；**文件内不写 dt/date 列**——防 DuckDB hive 遮蔽事故重演；列=
  symbol(后缀式)/ts/涨跌停/五档全字段/refresh_time/source，缺值 null 不假填）；
  ② 归档器 `SnapshotArchiver` 内嵌订阅引擎写侧（零额外读取；缓冲按日分组、行数/时间双阈值
  flush；Redis 写与归档**两路独立**互不阻断；flush 失败只计数；停机终刷不丢缓冲；
  文件名支持分片 tag——多 worker 并存防同毫秒撞车）；
  ③ 读取 `read_day`（DuckDB 单日目录 glob + symbols 过滤 + ts 排序。**偏离原案**：不经
  `hub._read_partitioned`——该函数硬绑 QuantDB `dt=` 分区；本库单日直读无 hive 枚举，
  且范式守卫测试锁死"文件内无 dt/date 列"，遮蔽风险归零）；
  ④ 质检 `quality_report`（逐标的单调/缺口/五档完整性 + flags）；⑤ 降冷 `prune_old`（只匹配
  `date=\d{8}` 目录，dry-run 可查；worker 内每日自动执行一次，keep_days 默认 90）。
  **运维面**：`backend/scripts/l05_maintenance.py`——`report`（当日/最近 N 日质检报告落
  `<base>/_reports/quality-<day>.json`）/ `capacity`（逐日行数/文件/字节 + 保留期投影）/
  `prune`（默认 dry-run）。
  **容量标定（实测）**：zstd 全字段 ≈ 145 B/行；3s 节拍 × 527 只 ≈ 2.53M 行/日 ≈ **367 MB/日**
  → keep_days=90 ≈ **33 GB**（1s 节拍 ≈ 1.1 GB/日——开盘实测节拍后用 capacity 复测，超预算则降
  keep_days）；单标的回放读 22ms（40 文件/42 万行基准），性能用例断言 <1s。
  **测试**：11/11 绿——写读回环逐值＋**分区范式守卫**（date= 目录、文件内无 dt 列、DuckDB 合成
  date 列断言）＋质检规则＋降冷只删 date= 目录＋归档器按日分组 flush＋**真推送样本→引擎归档
  sink→落盘→读回逐值**（I）＋缺失日空读＋写侧排序守卫＋维护 CLI 冒烟＋10 万行读性能。
  **真机**：生产 worker 重启加载归档器（日志 `l05 archiver enabled dir=/data/l05_snapshots`，
  订阅 527 只持续运行），`subscription_status` 快照带 `archiver` 计数块。
  **待办（验收）**：连续 5 交易日全量落盘 + 每日质检报告 PASS + 容量复测 ≤ 预算表（开盘起自然累计）。
  **夜补（2026-09-17，实测驱动）**：**归档时效闸门** `ARCHIVE_MAX_AGE_S=300s`——只留实时到达
  的帧；夜盘陈旧重放帧不落盘（曾污染：09-16 分区仅 65 只收盘重放行 500 条，已清除；
  `archived_stale_skipped` 显式计数）。
  测试：U（质检规则）＋ I（造数→落盘→按日读回逐值→清理）＋ P（读取性能/分区裁剪）＋ D（乱序/缺档数据）＋ R。

- [~] **T-P6-05 新鲜度契约统一 + 端到端时延度量**（2026-09-17 凌晨落地，P95 实测待开盘）
  **落地**：`shared/freshness.py`——fresh/stale/unavailable 三级唯一谓词 `classify_age`（纯函数，
  边界显式：≤fresh 基准线 / ≤stale 窗口可用须标注 / 超出或 ts 缺失不可用；**未来戳 >5s 判不可用**
  ——防未来分数长期霸榜 ZSET，容差内视为 fresh 吸收时钟偏斜）。阈值唯一读取点 `quote_policy()`
  （新 env `QM_QUOTE_FRESH_WITHIN_S`/`QM_QUOTE_STALE_WITHIN_S`；旧名 SIM/PREFLIGHT 两个 env 降为
  兼容别名，**仅本模块读取**，其余处零残留由 G 测试锁死）。
  **四处散点收敛**（比原案多一处）：stream `remote_redis_source`（硬编码 60/300）→ 分级 + stale
  告警；simulation `redis_series_quote`（fetch_tick/ticks + parse_series_member 全面换策略入参，
  返回体增 `freshness` 标注）+ `execution_engine`（取价门禁）→ 唯一谓词；preflight
  `check_stream_series_freshness`（trade 预检共用）→ 分级就绪判定，details 增 `level`/
  `fresh_within_seconds`，两段探测循环收敛为 `_probe_freshest_series_age`。**边界语义变更（显式）**：
  可用性判定由「age < 阈值」改为「age ≤ stale 窗口」（恰好等于窗口线由不可用改为可用，四处一致）。
  **时延打点**：`shared/latency_metrics.py`——`LatencyRecorder`（窗口分位 + 阈值 flush，best-effort
  不抛）写 `intel:latency:{stage}`（HASH 最新窗口 p50/p95/max/avg/计数）+ `:series`（ZSET 趋势，
  rank 保 24h）；订阅采集写侧内嵌打点（源时间戳=TDX 帧水印 → 标准键可消费时刻，worker 停机终刷）；
  `subscription_status` 快照增 `latency` 块。**报表面**：`backend/scripts/latency_report.py`
  （stage 一览/趋势/JSON）。
  **测试**：13/13 绿——边界值（含未来戳容差/NaN/None）＋ env 优先级与别名＋**源守卫 G**（services
  层旧 env/硬编码 60/300 零残留、唯一实现单点）＋ Recorder 分位/阈值/失败不抛（假 Redis）＋
  preflight 三级语义与降级回退（假 Redis）＋**真 Redis 打点回环**（写入→read_latency→清理）＋
  SubscriptionEngine sink 接线（真推送样本）。相关回归 510 passed（2 项 stock_pool 既有基线失败，
  与本任务无关）。
  **待办（验收）**：开盘后 `latency_report` 实测 P95 <2s（总线建成后接入总线消费点；当前打点=标准键
  可消费点）；stale 比例观察（>300s 帧即数据缺口信号）。
  **夜补（2026-09-17，实测驱动）**：① **时延打点双通道**——全量档（端到端真相，含一切帧）
  + **新鲜档** `market_snapshot_fresh`（≤QM_LATENCY_FRESH_GUARD_MS 默认 300s）——夜盘/停牌
  陈旧重放帧（实测 p50≈9h：收盘后服务器持续推当日 15:30 快照）**不入验收口径**；
  ② **开盘验收工具** `backend/scripts/p6_acceptance_report.py`：A 分片健康 / B 热集落地率 /
  C 时延新鲜档 P95 / D 数据帧节拍 / E L0.5 留存 / F 快车道预算——一屏判定，闭市项如实 N/A，
  退出码可进 cron；③ 聚合修正：分片 `hot_set_size` 取 max 非求和（曾把 527 报成 3162）。
  测试：U（边界值）＋ G（三处旧阈值零残留）＋ I（打点链路真 Redis）。

### 批次二：热集与实时推理

- [x] **T-P6-06 热集定义服务（全用户持仓并集共享）**（2026-09-16 深夜落地+真机联调）
  **落地**：`shared/hot_set.py` 纯函数（优先级 持仓>异动>候选/去重保序/上限截断统计/非法跳过）；
  `live_trading/services/hot_set_builder.py`——持仓源=交易 Redis `simulation:account:*` 全用户扫描、
  候选源=engine_signal_scores 最新交易日 CN 口径 TopN（截面去重）、异动源=T-P6-14 接口占位；
  **原子替换**（tmp 集合 + RENAME；空集删除目标键）写 `qm:hot_set:symbols` + `:meta`（built_at/统计）；
  60s 常驻 worker（trade 服务注册 + 调度注册表 JobSpec + 心跳）。
  **真机 E2E（三组）**：① 多用户夹具（2 账户持仓 + 当日候选行）→ 并集断言/上限截断/空集替换 → 清理；
  ② **生产实测**：builder 对真实数据构建 `kept=527`（sources: positions=55 + candidates=500）；
  ③ **全环联动**：订阅开关置 true → 生产 worker 拉起 → **527 只全量订阅建立**（深夜无数据帧属
  关市正常，开盘自动流转）。
  测试：U（并集/优先级/截断/空池）＋ I（真库真 Redis 多用户/空替换）＋ G（键同源守卫）6/6 绿。
  目标：热集 = 全用户持仓并集 ∪ 候选池（当日推理 TopN）∪ 异动池；上限预算（默认 2000，超出按
  优先级截断并如实记录）；`_resolve_watchlist` 泛化为多用户；产出落 Redis 热集集合 + 供 T-P6-02 订阅。
  验收：并集正确性（多用户夹具）；上限行为显式；与采集层联动（新入池 1 个周期内被采）。
  测试：U（并集/优先级/截断纯函数）＋ I（真库造多用户持仓 → 热集断言 → 清理）＋ D（空池/超限）＋ R。

- [~] **T-P6-07 增量特征引擎（复用因子定义单实现）**（2026-09-17 凌晨落地：单源迁移 + 快车道 + 状态机，金样全绿）
  **落地**：
  ① **批量定义单源**：`shared/feature_defs.py`——`compute_features_for_group`/`FEATURE_COLS`/
  输入列契约常量自 `update_feature_parquet.py` **纯迁移（逐字节）**；脚本与
  `update_market_features.py` 改为从共享模块导入（G 测试锁死"脚本不得本地定义"）。
  迁移安全性证据：真实快照 5 标的 × 300 行 × 187 列迁移前后**逐位一致**（SHA256 金样）。
  ② **快车道** `shared/feature_incremental.py`：32 列 TIER（价格/量额/波动/资金流/位置，
  依赖窗 ≤45）numpy O(窗) 求值，**每列与批量同名输出逐值等价**——真实快照 20 标的 × 20 日
  ×32 列全量比对 **ε=1e-9 全过**（`test_tier_matches_batch_on_real_data`；停牌行过滤/NaN 语义/
  min_periods/clip 顺序逐一镜像）。**性能实测**：批量单标的（300 行全量 150+ 特征）≈112ms
  不可实时；快车道 ≈2.2ms/只 → 500 只 ≈1.1s/轮（15s 节拍预算 ~7%）。
  ③ **状态机** `inference/incremental_features.py`：历史窗口环 + 当日形成 bar（open=首帧/
  feed.Open，high/low 极值跟踪，close=最新价，volume/amount=feed 累计取最新不求和）
  + 分钟桶环（ts//60，供 T-P6-13）；显式策略全覆盖：乱序整帧拒绝/同日重复幂等/缺口计数/
  日切入环/**同日历史遮蔽**（引导含当日行时计算剔除防重复计日）/冷启动标记；
  `features_with_fallback` 输出 (全量行, provenance)：live 覆盖 T-1 基线，live 缺失自动回退
  基线并标注 t1/cold（模型输入不因单列缺口整体降级）。
  **口径核查发现（T-P6-08 前置结论，重要）**：以快照自带 OHLCV 反推生产列口径——
  `mom_ret_1d/5d` 与 pct_change **精确一致**（正常标的）；`vol_std_20` 量纲/算法不同
  （快照值 ≈ 我们的 ×100 且非恒定比）；`liq_volume_ma_20/liq_amihud_20/flow_vpin` 等快照列
  近全 NaN；快照符号形态仍有 suffix 污染残留（≈4 行/只）。→ **T-P6-08 实时覆盖只允许白名单列**
  （先按此表逐列裁定，防量纲漂移写入模型输入）。
  **测试**：7/7——真实数据金样（核心验收）＋冷启动截断窗等价＋注册表/单源守卫＋
  聚合/日切/遮蔽/乱序/重复/缺口＋provenance 回退＋真实推送样本帧状态演化。
  **待办**：tier 扩展（EMA/RSI/KDJ 状态携带——无限记忆特征需跨调用状态）；快照列口径逐列
  裁定表（决定 T-P6-08 覆盖白名单）；分钟级特征（分钟桶已就绪，待 T-P6-13 消费）。
  测试：U（与批量口径金样对齐——本任务核心断言）＋ I（真快照流喂入 → 状态演化断言）＋ D（乱序/缺口）＋ G（定义单实现）。

- [ ] **T-P6-08 ONNX 导出链 + 热集推理服务**
  目标：训练产物 → ONNX 导出器（pkl/lgb/pth → onnx，含校验=原模型 vs ONNX 逐值等价）；
  常驻热集推理进程（节拍可配：首版 **15s**，目标态 3s——见 §六.2）；信号经
  `POST /engine/runs/{run_id}/signal-ready`（现成端点）写 Signal 契约 `source=realtime` + `signal_ts`；
  单实例锁复用 `inference_lock`。
  验收：500 只单轮推理 <1s（P95）；信号落 `engine_signal_scores`（source=realtime）且被
  SignalLoader 正常读取；`ENABLE_SIGNAL_STREAM_PUBLISH` 开启并压测后给结论。
  测试：U（导出等价逐值/契约字段）＋ I（真库：热集 → 推理 → 信号行断言 → 清理）＋ P（单轮延迟/常驻内存）
  ＋ D（模型缺失/特征缺口/节拍过载自保）＋ G（`source=realtime` 唯一写入面）＋ R。

- [ ] **T-P6-09 可回放复现验收器（P6 硬验收）**
  目标：脚本+回归用例——给定留存快照日 + 模型 + 策略：离线重放实时特征与推理，产出信号与
  实时链路留档 diff=0；不一致视为 bug（失败即阻断）。
  验收：至少 2 个真实交易日全流程 diff=0。
  测试：U（重放确定性）＋ I（真实留存数据回放）＋ G（验收器进回归清单）。

- [ ] **T-P6-10 单容器资源预算与降级自保**
  目标：P6 各进程 CPU/内存预算表（先算账）；过载自保（节拍降级/暂停实时推理只保行情与风控，
  如实标注）；`status` 暴露预算占用。
  验收：压测在预算内（推理 500 只 + 采集 300 只 + 服务常驻）；过载注入时降级路径生效可回切。
  测试：U（预算判定）＋ P（压测实测存档）＋ D（过载注入）。

### 批次三：盘中情报总线

- [ ] **T-P6-11 事件契约 + 总线本体（Redis Stream）+ 既有缺陷修复**
  目标：`intel:events` Stream（设计 §6.3 schema：ts/type/market/targets/level/payload/actions_hint）；
  生产者/消费者 SDK（统一投递点）；WS 新 topic `intel.{tenant}.{user|market}` **并补齐 topic 鉴权**
  （§4.1-4 安全缺口）；**顺带修复**：`data_quality` 通知链路（列名/SERIAL 错）、alerts 表接线。
  验收：三类事件（news/regime/anomaly）端到端落总线并被消费可观测；schema 校验拒绝畸形事件；
  WS 越权订阅被拒（测试断言 1008/SUBSCRIPTION_FORBIDDEN）。
  测试：U（schema 校验/序列化）＋ I（真 Redis：投递→消费→留痕）＋ D（畸形/超大 payload）＋ 安全用例
  （跨用户订阅拒绝）＋ R（notification 全链回归）。

- [ ] **T-P6-12 新闻情绪分钟级接入（实体链接 + 事件分级 + veto 直连）**
  目标：PG enrichment → 去重/时效过滤 → 实体链接（个股/行业）→ 事件分级（中性/利好/利空/风险事件）
  → 总线事件；风险事件 → 风控 veto 直连（策略配置 `risk.veto.news_event=true` 生效）；情绪突变 → 告警。
  验收：分级金样集全对；veto 真实拦截（当日事件 → 买单被拒留痕）；误判修正回填路径可用。
  测试：U（实体链接/分级金样）＋ I（造新闻 → enrichment → 事件 → veto 拦截断言）＋ D（空/重复/过期新闻）＋ R。

- [ ] **T-P6-13 市场状态服务（日内 regime，3s–1min）**
  目标：指数趋势/涨跌家数/涨跌停家数/成交额/量比/板块相对强度 → 日内 regime；**与日频
  `market_state` 同口径同枚举**（复用 `market_breadth.py` 纯函数）；输出总线 `regime` 事件 +
  Redis 快照；供风控（仓位系数）与策略（开关）消费。
  验收：同日收盘后：日内 regime 终值与日频 regime 一致（口径一致性断言）；盘中 3s 级更新无积压。
  测试：U（口径一致：同一输入两侧逐值同）＋ I（真行情流 → regime 演化）＋ P（3s 更新延迟）＋ R。

- [ ] **T-P6-14 识别引擎 v1（异常检测 → 三类动作）**
  目标：四类检测（市场异动-量价/账户异常-撤单率与集中度/数据异常-跳变缺口/模型异常-IC 骤降
  [接入 model_ic_monitor 调度]）→ 动作（告警/否决/降仓）→ 总线 + 风控接线（fail-closed）；
  **复活死代码**：`rule_engine.create_anomaly` 接线、`qm_market_anomalies` 开写。
  验收：四类各 ≥1 个真实场景触发（或确定性夹具）；动作真实生效（否决留痕/降仓可审计）。
  测试：U（检测规则金样：每类正反例）＋ I（造数触发 → 动作断言 → 清理）＋ D（噪声不误触发）＋ R。

### 批次四：哨兵推送与副驾驶

- [ ] **T-P6-15 哨兵推送（分级 + 留痕 + 误报闭环）**
  目标：总线事件 → 分级推送（复用 notification 管道；notifications 扩 symbol/severity/来源列或
  payload JSON）→ 前端实时 + 通知卡；**告警留痕表**（事件/动作/结果）+ **T+1 自动回填**（命中=
  告警后标的表现符合预期，复用 `daily_selection.score_realized` 范式）+ 人工标注入口 → **误报率报表**。
  验收：误报率口径落表可查（口径见 §六.4）；标注→报表闭环 E2E；分级推送不过载（节流/聚合）。
  测试：U（误报率计算/回填规则）＋ I（告警 → T+1 回填 → 报表断言 → 清理）＋ D（无结果样本）＋ R。

- [ ] **T-P6-16 副驾驶建议（实时上下文 + 操作建议卡）**
  目标：QuantBot 注入实时上下文（持仓/热集信号/当日告警——工具化查询，非 prompt 拼接）；
  建议卡（accept/reject/一键走 OrderRouter 执行，带来源与依据下钻）；交易台实时面板
  （替换 `IntelligenceAlertsCard` mock：展示总线事件流 + 误报标注入口 + 时延/预算指标）。
  验收：建议卡可下钻到原始事件/信号；一键执行走 OrderRouter 且留痕（source=co_pilot）；面板无 mock。
  测试：U（意图→建议映射金样/组件渲染）＋ I（建议→执行链 E2E）＋ 前端组件测试 + typecheck。

### 批次五：F2 快照级撮合

- [ ] **T-P6-17 SnapshotCore v1（ExecutionCore SPI 落地）**
  目标：快照级撮合内核：现价 + 盘口五档深度约束（吃穿档位计算可成交量）+ 涨跌停排队 + 部分成交；
  与 DailyCore 可切换（`exec_core=daily|snapshot`，交易台标注保真度级别）；复用合流后的
  `_reserve_liquidity`（参与率）为兜底约束，**盘口深度可用时以盘口为准**。
  验收：深度受限场景金样（下单量 > 盘口量 → 部分成交 + 余量排队）；与 F1 平价开关可回退；
  成交价合规（涨跌停带内钳制）。
  测试：U（撮合金样 ≥10 例：穿档/部分/涨跌停/停牌）＋ I（真快照回放 → 撮合断言）＋ D（空档/陈旧快照）＋ R（规则平价）。

- [ ] **T-P6-18 F2 保真度度量上交易台**
  目标：三指标持续度量（成交价偏差分布=模拟 vs 当日真实价、成交率、部分成交比例）+ 滑点实现值
  vs 配置值；接 `eval_scores`/desk。
  验收：指标口径文档化 + 面板可见（无数据如实标注）。
  测试：U（指标计算）＋ I（造配对样本 → 断言）＋ 前端组件测试。

- [ ] **T-P6-19 影子对照扩展（F2 模拟 vs 实盘同单）**
  目标：现有 shadow compare 增 F2 维度（快照级模拟单 vs QMT 实盘同单：价格/成交时点/部分成交差异）；
  日报扩展。
  验收：同单配对率/偏差在日报可见；异常样本可下钻。
  测试：U（配对逻辑）＋ I（造 sim + 镜像单 → 日报断言）＋ R（既有影子回归）。

---

## 六、开放问题（待拍板）

1. **热集范围与上限**：持仓∪候选∪异动（设计建议 300–800，上限 2000）。默认取设计建议 + 2000 上限，
   有异议请指定。
2. **推理节拍**：首版 **15s**（单容器预算内稳妥），3s 为目标态（T-P6-10 压测后再定）——若要求
   首版即 3s，请明示（资源预算需上探）。
3. **快照留存范围**：默认 **热集 3s + 五档**（全市场 3s 属 P7/L2，磁盘与带宽不支持）。
4. **哨兵误报率口径**：建议 = 告警后 T+1 标的表现未朝告警方向走（自动回填）× 人工复核修正；
   达标线建议首版 ≤30%（先跑数再收紧）。请拍板口径与线。
5. ~~快照行情源~~ **已决（2026-09-16 用户）：TdxAiData（免 Windows，前端可配），桥为增强**。

---

## 七、风险与预案

| 风险 | 预案 |
|---|---|
| TdxAiData Token 限流/会员中断 | 指数退避 + status 标 degraded + 回落桥/免费行情服（消费方走既有新鲜度分级）；自检按钮暴露 |
| 单容器资源耗尽（采集+推理+服务同容器） | T-P6-10 预算表 + 过载降级（保行情风控、暂停推理）；节拍可配置热调 |
| 实时信号与批量信号冲突 | 同表契约、`source` 区分、`signal_ts` 精度排序；策略消费以 `rank_pct` 为准（既有口径） |
| 实时链路静默失效 | 全链心跳进 `scheduler_registry` + 时延/新鲜度指标进交易台；验收器（T-P6-09）上回归 |
| 快照留存磁盘增长 | 容量预算表 + 降冷脚本 + `data/` 水位监控（根分区 84% 现状先解决） |
| WS 越权（安全） | T-P6-11 前置补齐 topic 鉴权（现状 `stock.*/strategy.*` 无校验） |
| 与 P7 边界蔓延 | 本文「明确不做」清单为界；tick/L2 需求一律转 P7 评估 |
