# MVP 实施计划 · 前端（勾选即进度）

> 版本：v1（2026-09-15）　配合：`docs/前端界面设计_设计方案.md`、`docs/图表与可视化_设计方案.md`、`docs/MVP实施计划_后端.md`
> 使用方式同后端计划：任务 = 代码 + 测试 + 验收，勾选即进度；提交信息带 ID（如 `feat(FE-02): ...`）。
> 状态图例：`[ ]` 未开始　`[~]` 进行中　`[x]` 完成　`[-]` 推迟

---

## 总览看板

| 阶段 | 进度 | 依赖 |
|---|---|---|
| FE-A 基础层（术语/模式/下钻） | 3/3 ✅（术语层/模式开关/下钻抽屉 v1；扩展接入随各页面推进） | 无（可并行后端 P0） |
| FE-B 今日交易台 v1 | 3/3 ✅（骨架/健康卡/计划卡+一键执行） | 后端 T-P1-05 ✅ |
| FE-C 图表专业层 | 3/3 ✅（复权角标/买卖点下钻/水下图+区间统计；性能与双主题走查随下批） | K 线接口（现有）+ FE-A |
| FE-D 策略中心（写策略/提要求） | 4/4 ✅（版本与变更 / 模板库 / 门槛可见 / 四类意图示例；AI 写策略对话细部对齐随 AI-IDE） | 后端 P3 + AI 接口（现有） |
| FE-E 评估与证据（评分卡/体检） | 3/3 ✅（评分卡组件+三处内嵌/体检页签+自助上传/十环证据矩阵） | 后端 T-P4-05/P4-06 ✅ |
| FE-F 合规与安全体验 | 2/2 ✅（合规四件接线 / 危险确认卡四处覆盖+分散提示+止损锁定展示） | FE-B |

**总约束**：外观与现有界面统一（沿用现有组件/tokens）；接线只动 `App.tsx` + `api/main.py`；构建走 `web/dist` 预编译流程；任何任务评审附加一问"用户几步用到"（>3 步重设计）。

---

## FE-A 基础层（可与后端 P0 并行）

### T-FE-01 术语翻译层
- **内容**：`electron/src/features/shared/glossary.ts`（唯一来源：术语 key → 人话 + 说明）；`TermTooltip` 组件；覆盖全部对外指标（rank/IC/回撤/滑点/体检标签等）
- **验收**：简单模式下界面无裸术语（走查清单）；tooltip 接入 3 处试点
- **测试**：glossary 覆盖度单测（所有 UI 使用的术语 key 必须有翻译）
- **设计**：前端设计 §一.3

### T-FE-02 简单/专业模式开关
- **内容**：全局模式 state（持久化）+ 各面板按模式渲染；默认简单
- **验收**：切换后交易台/策略卡/个股页 3 处正确降/升维
- **测试**：模式切换渲染快照测试
- **设计**：前端设计 §一.2

### T-FE-03 下钻抽屉（通用组件）
- **内容**：`DrillDownDrawer`：任意数字 → 来源链（曲线点→快照→订单→成交→信号→机会）
- **验收**：交易台抽查 10 个数字全部可下钻到最底层
- **测试**：组件单测 + 与后端 source 字段契约测试
- **设计**：前端设计 §一.4

---

### 落地记录（2026-09-16 四批 · FE-A 基础层）
- **T-FE-01 术语翻译层 ✅**：`features/shared/glossary.ts`（**28 术语唯一来源**：信号 rank_pct/
  fusion_score、体检九项、四分类 A/B/L/E、评分红线/低置信†、执行 dry_run/退出规则/滑点/
  跟踪误差、source/pipeline；每条含人话 + 机构口径）+ `TermTooltip`（简单模式只显人话，
  专业模式追加机构说明）。**覆盖度单测**：扫描全仓 `term="xxx"` 字面量必须都有翻译（漏翻即红）
  + 核心术语清单齐备 + 翻译非空不重复。已接入 3+ 处（交易台 rank_pct/预演/退出规则、体检九项
  行与结论、评估中心）。
- **T-FE-02 简单/专业模式开关 ✅（基础设施 + 3 面应用 + 2 处收尾）**：`uiSlice.uiMode`（默认简单、
  localStorage 持久化、非法值归一）+ `useUiMode` + `UiModeToggle`（**页面内落点**：首页
  HeaderBar / 交易台页头 / 技能中心页头；曾试挂底部导航，按反馈撤出——全局悬浮栏不放
  模式开关）。应用：**体检面板**（简单=结论+理由+建议精炼版，
  专业=九项明细——补上 T-FE-15 的"简单模式三句话版"）、**评估中心**（简单=徽章+分数，
  专业=雷达+历史+维度明细）、**交易台**（简单收起 source 技术来源，下钻入口保留）。
  **收尾（2026-09-16）**：**策略卡**——简单模式收起 v 版本 chip/参数锁/EvalScoreBadge/创建时间/
  「版本与变更」按钮（保留晋级门槛作"下一步"引导）；**个股页**——三市场详情页签按
  `visibleDetailTabs/fallbackDetailTab`（stock-terminal-shared 纯函数）过滤：A股简单=概况/财务/
  估值/资讯（收起筹码/融资/形态/股东/L2）、港股收起 CCASS/南向/分析师、美股收起分析师/财报/
  内部人/机构，被收起的当前页签自动回落 + 提示"顶栏可切换专业模式"。
  **余**：设计文档「设置→模式切换」待通用设置页落地时补位（经查 user-center 的
  `pages/SettingsPage.tsx` 为未挂载死文件，当前设置面均为 LLM 密钥/接入凭证）。
  实测（3080 部署版）：专业态 74 张策略卡全量（版本与变更/创建时间可见）↔ 简单态 0 张；
  个股页 9 页签 ↔ 4 页签 + 提示，四象限全部按预期。
- **T-FE-03 下钻抽屉 ✅（v2 逐层穿透）**：`DrillDownDrawer`（条目化来源链 + 原始载荷 JSON 可复制
  + **层级栈/面包屑/返回上一层**；条目 `drill` 携带下一层，每层原始载荷随层切换）。
  接入面：交易台 计划/盈亏（v1）+ **管线四步**（步骤 → 同源体检断言证据环 → 逐证据项）+
  **候选信号条目**（字段 → 原始条目 → 信号块载荷）+ **执行条目**（订单字段 → 取价来源说明 →
  原始条目）+ **计划单**（单字段 → 触发类别/对应当日信号 → 原始条目，退出单/调仓单权限说明不同）。
  实测：结算/台账 → 账本证据环（C04/C05 逐项）→ 返回 → 面包屑回根；信号 301223 L1（rank_pct 1.000）
  → L2 原始条目（rank_pct 0.9996 原样，四舍五入只发生在 L1 展示层）。
- **验证**：vitest 新增 **10/10**（glossary 覆盖度 4 + uiMode 4 + 下钻条目 2）；全量前端
  436 passed / 22 failed——与**清树基线 426/22 逐条对照，零新增失败**（+10 全部新测）；
  typecheck 零错误；浏览器探针实机：tooltip 双模式内容差异、抽屉（含原始载荷）、
  评估中心专业 canvas=1 ↔ 简单 canvas=0，**零 console 错误**。

## FE-B 今日交易台 v1

### T-FE-04 交易台页面骨架
- **内容**：新首页：管线进度条（数据✓→特征✓→推理✓→信号✓→计划✓→执行→结算）＋四卡片（候选/调仓计划/执行/盈亏）＋异常红点；数据源 `/desk/today`
- **验收**：一屏看全当日闭环；空态引导文案齐全
- **设计**：前端设计 §二、评估体系 §八

### T-FE-05 调仓计划卡（含理由）
- **内容**：买卖清单（每笔 reason + 触发规则）、人工可审/可调、一键执行（走确认卡）
- **验收**：计划→执行全流程可用；每笔理由可下钻
- **设计**：持仓执行文档 §一

### T-FE-06 系统健康卡
- **内容**：12 项断言红黄绿 + 下钻体检报告（与 T-P0-07 同源）
- **验收**：注入故障后卡片变红并给出建议
- **设计**：可维护性 §四

---

### 落地记录（2026-09-16 三批 · FE-B）
- **T-FE-04 交易台骨架 ✅**：`features/desk/`（路由 /desk + 底部导航「今日交易台」）——管线四步
  进度条（与体检同源）+ 四卡（候选信号/调仓计划/今日执行/账户盈亏）+ 系统健康 chips；
  每块 source 下钻脚注；实机 200 全真实数据（信号 1040 BUY / 总资产 201 万 / 健康 5-4-1）。
- **T-FE-05 调仓计划卡 ✅**：**预演展示**——后端 `/desk/today.plan` 由引擎 **dry-run**
  现算（与执行同一 RebalanceCalculator；绝不撮合/落单/写快照），每笔带理由 + 触发类别
  （定期调仓/退出规则）+ 涨跌停/停牌标注 + 预估金额；无活跃策略时如实空态。
  **一键执行**为确认卡 + 三重闸门（见五批记录）。
  **人工可调（v2，2026-09-16）**：确认卡逐笔数量输入（默认计划量、实时预估金额、一键恢复；
  非法输入（≤0/非整数）拦截确认按钮）——**改量契约（机构口径）**：`apply_quantity_overrides`
  （引擎唯一实现）——退出规则单不可改量（风控动作不绕过）；未命中当前计划/归一失败的条目
  **逐条裁定进 report.quantity_adjustments**（不静默、不猜测、不补单；页面如实展示"改量未应用+原因"）；
  数量经 `normalize_order_quantity` 归一（科创板 200 起 1 股递增、主板整手）；**不改价格/方向/不新增单**；
  API 载荷 fail-fast（400，资金相关调整不静默降级）。单测：解析矩阵 12 例 + 引擎裁定 6 例 +
  真 RebalanceCalculator 全链 1 例（退出单同轮并存）+ API 透传/400 + 前端载荷/门禁组件 5 例。
  **真机全链 E2E（2026-09-16 晚）**：修复体检链两处静默失效（见后端计划同批事故记录）后——
  4 年窗回测 → 体检 **A/88**（alpha t=2.11，DSR 0.975）→ 门禁放行启动策略 2 → 交易台计划
  **77 笔** → 确认卡改量 002552.SZ 卖 500→600 → 执行 HTTP 200，`report.quantity_adjustments`
  =[applied:true]，UI 明示"改量应用：002552.SZ 卖 500 → 600"；**PG 实锤**：
  `sim_orders: SZ002552 sell 600`（落库数量=改量后值而非计划值 500；盘后拒单属撮合时段规则）。
  **前置**：侦察实机修复托管执行链三处 P0 静默断链（见后端计划同批记录）。
- **T-FE-06 系统健康卡 ✅**：体检 10 项断言红黄绿 chips + hover 下钻（detail/suggestion），
  与 `diagnose/health.py` 同源。
- **测试**：`deskModel` vitest **8/8**（管线最差态/计划买卖分组与金额/退出规则计数/盈亏容错/
  健康视图/格式化）；`npm run typecheck` 零错误；浏览器探针：真实数据零 console 错误 +
  网络层注入验证计划卡满态渲染（买1卖2/退出规则标注/金额合计）。

## FE-C 图表专业层

### T-FE-07 K线专业组件
- **内容**：接入 klinecharts（或同等）：蜡烛/量/均线/指标叠加/十字光标/区间缩放；复权口径角标；红涨绿跌
- **验收**：图表规范十条走查全过（K 线部分）
- **测试**：注入缺失数据不画假线；1 万根 K 线缩放流畅
- **设计**：图表可视化 §二

### T-FE-08 买卖点标注 + 下钻
- **内容**：B/S marker 渲染（系统实际成交点）＋点击弹出"为什么"（信号分/规则/执行价）
- **验收**：回测/模拟/实盘三处图表买卖点与订单一一对应
- **设计**：图表可视化 §二.4

### T-FE-09 资金曲线 + 回撤水下图
- **内容**：净值 vs 基准、回撤水下、区间统计；与交易台联动
- **验收**：曲线数字与账户卡一致（一致性断言）
- **设计**：图表可视化 §一

---

## FE-D 策略中心

### T-FE-10 策略列表与详情
- **内容**：状态机展示（DRAFT→VERIFIED→SIM→LIVE）、生效参数及来源、版本 diff、参数锁标记
- **验收**：改参→升版本→diff 全流程可用
- **设计**：前端设计 §四

### T-FE-11 模板库与"复制为我的策略"
- **内容**：minibt 模板（11 个）列表 + 三行说明 + 一键复制
- **验收**：新用户从模板到回测 ≤3 步

### T-FE-12 AI 写策略对话
- **内容**：自然语言 → 引导澄清 → 结构化草稿（params/minibt）→ 校验 → 一键回测 → 证据卡 → 晋级按钮（门槛达成可见）
- **验收**：无文档完成"描述→上线模拟"全流程
- **设计**：前端设计 §四、LLM 方案 §三

### T-FE-13 提要求统一入口
- **内容**：全局 AI 助手：四类意图（写策略/选股筛选/分析问答/操作帮助）；写操作两步确认卡；输出带数据来源脚注
- **验收**：四类意图各有可用示例；确认卡阻断直达执行
- **设计**：前端设计 §五

---

### 落地记录（2026-09-16 六批 · FE-D 主体）
- **T-FE-11 策略模板库 ✅**（技能中心新页签）：95 套（minibt×11）三行说明 + 难度/市场/关键词
  筛选 + 代码折叠；「立即回测」经 `_perform_sync` 镜像策略走既有预选机制——**模板→回测 2 步**
  （实机：点卡 → 回测中心 → 快回已载入所点模板镜像）。容错映射唯一边界（后端两代字段都认）。
- **T-FE-12 晋级门槛可见 ✅**：策略卡「晋级门槛」Popover——门禁预演（与启动端点同判定）+
  体检结论/可信度/理由/建议 + 结论历史；实机真实 200 + 拦截文案。
- **T-FE-10 版本与变更 ✅**：后端 `strategy_versions` 契约（保存同事务快照、幂等、安全三纪律）
  + `/strategies/{id}/versions` 端点；前端抽屉（参数差异表/代码行级 diff LCS/生效参数及来源
  ——与模板默认比对）；策略卡 v 版本 chip + **参数锁标记**；修 normalizeStatus 规范词识别
  （VERIFIED/SIM/LIVE 曾误显"草稿"）。
- **T-FE-12 剩余**：AI 写策略对话的"门槛达成可见"随 AI-IDE 流程细部对齐（当前门槛卡已全局可用）。
- **T-FE-13 四类意图示例 ✅**（第 581cf56c 批）：QuantBot 活壳示例条（写策略/选股筛选/分析问答/
  操作帮助 ×3 示例，chip 点击复制提示词）；`needsTwoStepConfirm`/`confirmConsequence` 判定模型
  ——已随 T-FE-18 收敛为 `shared/compliance/dangerAction` 唯一实现（quantbotIntents 再导出兼容）。
  写操作两步确认卡由 FE-18 的 DangerConfirmModal 全面接管（聊天界面仍不提供直达下单按钮）。

## FE-E 评估与证据

### T-FE-14 评分卡组件（雷达+历史）
- **内容**：模型/策略/选股卡片：五维雷达、历史分曲线、A/B/C/D、低置信标注
- **验收**：三处卡片（模型/策略/选股）统一组件渲染
- **设计**：评估体系 §二

### T-FE-15 回测体检报告页
- **内容**：四分类标签 + 九项检验 + 简单模式三句话版；自助上传入口（净值曲线/交易日志）
- **验收**：上传→出报告全流程可用
- **设计**：评估体系 §六

### T-FE-16 全链证据面板
- **内容**：十环证据矩阵一屏（红黄绿+下钻报告原文）
- **验收**：任一环节"无证据"可见
- **设计**：评估体系 §七

---

### 落地记录（2026-09-16 二批 · FE-E 部分）
- **后端数据出口**：`/api/v1/eval/*`（`services/api/routers/eval_scores.py`）——scores 网格
  （latest_only）/ scores/history（升序）/ health/{sid}（最新+历史+**晋级门禁预演**·与执行点
  promotion_gate 同源）/ object-types；可见性=租户内共享行（user_id=''）+ 本人私有行；只读。
  测试 `test_eval_api.py` **5/5**（纯函数 + 路由注册/可见性源断言 + 真库 E2E）；HTTP 实机冒烟 200。
- **T-FE-14 ✅**：`EvalCenterPanel`（技能中心「评估中心」页签）——五类卡网格
  （A/B/C/D + 低置信 † + 红线标注）+ 多维**雷达** + **历史分曲线** + 维度明细（缺省/红线如实）；
  纯函数 `evalCenterModel.ts` 单测 **7/7**；三处页面内嵌（模型管理/策略管理/交易台候选信号，
  EvalScoreBadge）已随五批收口落地。
- **T-FE-15 ✅**：回测结果「体检结论」页签（`HealthEvidencePanel`，已迁移复用 `HealthReportView`
  统一视图）+ 简单模式三句话版 + 自助上传入口（净值曲线/交易日志 → 九项报告，只读自查）。
- **T-FE-16 ✅**（第 318a1f31 批）：十环证据矩阵一屏（/desk/today.evidence 聚合）——
  无证据 ≠ 绿（独立灰态）、每格下钻、绕级取最差；实机 5 正常/3 警告/1 异常/1 无证据可见。

### 落地记录（2026-09-16 五批 · FE-B收尾/FE-C/FE-E收口）
- **T-FE-05 一键执行 ✅**：后端 POST /desk/plan/execute——**三重闸门**（须活跃策略且仅模拟盘 /
  同一策略 60s 防重 NX 锁（Redis 不可用 fail-closed）/ 退出规则单不可人工排除）+ 唯一执行入口
  复用；前端确认卡（口径三行 + 逐笔勾选排除 + 计数实时 + 结果区）。实机含**真实 409 闸门**路径。
- **FE-C 图表专业层 ✅**：修复**两处 lightweight-charts v4 幽灵调用**（EquityCurve/
  通用 KlineChart——库升 v5 后 addLineSeries/addCandlestickSeries 移除、`as any` 吞错致
  长期静默空白；组件级 mock 回归测试锁死 v5 路径）；个股终端 K 线**复权口径角标**；
  **买卖点标注 + 点击下钻**（新后端 /stock-terminal/trade-marks 三形归一 + 理由透传；
  信号三角与分数副图同源下钻）；资金曲线重写（基准 + **回撤水下图**（v5 pane）+
  **区间统计**（收益/年化/MDD/波动/夏普/超额；数值噪声守卫））。
- **FE-E 收口**：**自助体检**（POST /eval/health/upload：CSV/JSON 解析纯函数 + 唯一体检实现；
  只读自查不落档案不参与门禁）+ **评分卡三处内嵌**（EvalScoreBadge：模型管理/策略管理/交易台
  候选信号；模型 id sys- 前缀归一；无记录灰态「未生成」）+ HealthReportView 抽共享
  （回测体检页签与自助体检同视图）。
- **验证**：后端各批真库 E2E/守卫测试（+6 执行 / +3 trade-marks / +3 上传）；前端 vitest
  累计 451 passed（22F=基线，零新增）；三批浏览器探针全绿（含 409/200 真实路径、三徽章三态）。

- **T-FE-16 全链证据矩阵 ✅**（第十一提交批）：`/desk/today.evidence` 十环聚合
  （数据/特征/模型/信号/回测/模拟/执行/账本/策略/系统 = 体检断言 + 评估留档 + 对照日报 +
  特征分区新鲜度）——**无证据 ≠ 绿**（空源独立 no_evidence 灰态，验收"任一环节无证据可见"）；
  绕级状态取最差滚动。前端 EvidenceMatrix（交易台新区块）：十环网格 + 无证据名单横幅 +
  每格下钻（证据项 detail/建议/来源 + 原始载荷）。实机：5 正常/3 警告/1 异常/**1 无证据
  （回测）可见**（特征滞后 20260914 vs 信号日、模型最差 B、账本缺口等真实摘要），零 console 错误。

## FE-F 合规与安全体验

### T-FE-17 合规四件
- **内容**：首启风险问卷（适当性）、收益展示规范组件（区间/口径/提示）、免责文案、留痕埋点
- **验收**：新用户注册路径全量覆盖；收益展示走查合规清单
- **设计**：产品化 §四

**落地记录（2026-09-16，FE-F 一批）**：
- **首启风险问卷**：`components/shared/compliance/RiskProfileModal.tsx` + `riskProfile.ts`（5 题 / 计分分档
  ≤2 稳健・3-6 平衡・≥7 进取 / localStorage 留痕）。App.tsx 在已登录且无档案时弹出（"稍后再答" 7 天内
  不打扰、到期重问）；高风险动作不受问卷状态影响，恒走确认卡。
- **收益展示规范**：`ComplianceReturn`（数字 + 样本区间 + "过往不代表未来"）接入交易台账户盈亏卡的
  累计收益率、仪表盘资金概览卡的收益口径脚注（口径 + 截至快照日）。
- **免责文案**：`ComplianceFooter`（量化研究工具/不构成投资建议/不代客理财 + 已评估时的风险等级）
  挂载三处——仪表盘首页常驻、今日交易台页底、交易页侧栏（真实资金操作区）。
- **留痕**：`complianceLog.ts` 本地环形留痕（上限 200 条）——问卷提交/跳过、危险操作确认/取消
  （DangerConfirmModal 内统一记录，接入点零改动）。**边界如实**：等级与留痕 v1 暂存本地，跨设备/服务端
  留档需后端用户档案接口（后端 P4 评估留档/审计日志已另行覆盖信号与执行侧）。
- **收益展示走查合规清单**：✅ 交易台累计收益率（区间+免责）、✅ 仪表盘总收益率（口径+免责脚注）、
  ✅ 免责页脚三处覆盖；回测/评估中心结果页由体检报告自带的样本区间与统计口径说明覆盖（FE-E 已落地）。

### T-FE-18 默认安全交互
- **内容**：危险操作二次确认（清仓/关风控/上实盘/大额）、强制分散提示、止损默认开启展示
- **验收**：四类危险操作全部有确认卡且文案说清后果
- **设计**：产品化 §五

**落地记录（2026-09-16，FE-F 一批）**：
- **统一确认卡**：`DangerConfirmModal` + `dangerAction.ts`（场景文案库：后果/不可逆性/撤回方式，重点加粗；
  确认前绝不触发动作）。
- **四类危险操作覆盖**：① **上实盘**——`useTradingModeSwitch` 统一入口（顶栏与交易页侧栏同源，
  修 RealTradingPage 此前绕过确认的缺口）；② **大额调仓**——交易页手动任务「推送执行」买卖预估总额
  ≥20 万时先过确认卡（纯卖出按"含清仓风险"提醒）；③ **清仓**——本版无独立清仓按钮，UI 中以
  "全部卖出"形态出现即走大额/纯卖出确认路径（诚实口径）；④ **关风控**——止损在设计上**平台锁定不可关闭**
  （-3%~-20% 仅可调幅度），表单与向导均标注锁定；`disable_stop_loss` 场景文案已入库，供未来
  合法降级路径复用。删除策略（破坏性）并入确认卡。
- **强制分散提示**：`diversification.ts` 纯函数（单票 ≤15% / 最少 3 只，按本次计划买入金额估算）——
  交易台调仓计划卡展示"分散检查 ✓"或琥珀警告条。
- **止损默认开启展示**：实盘配置表单 `全局止损（平台锁定：不可关闭）` + 向导风控行
  `止损 X%（平台锁定·不可关闭）`。

**实测证据（2026-09-16 本机 3080 部署版浏览器探针，`probe_fef.mjs` / `probe_stoploss_lock2.mjs`）**：
首启问卷弹出→作答→本地留痕（balanced/score=5）✅；仪表盘页脚含资质边界+风险等级 ✅；顶栏切实盘
出确认卡（文案含"真实资金"），取消后偏好未写 real、确认后生效 ✅；交易页侧栏切实盘同样出确认卡 ✅；
交易页页脚 ✅；交易台页脚 ✅；账户盈亏卡显示 `0.51%（2026-09-16）过往不代表未来` ✅；
部署向导实开：`全局止损（平台锁定：不可关闭）` 可见 ✅。
**未覆盖（如实）**：手动任务「推送执行」的大额确认卡因向导多步表单（模型→批次→策略）E2E 成本高，
本批以纯函数单测（阈值/文案/纯卖出清仓提醒）+ 组件测试（确认卡渲染/取消留痕）覆盖，UI 全链留待
下次交易时段人工走查；清仓无独立按钮（见上条口径）。
单元测试：`compliance.test.tsx` 22/22 + `PlanCard.test.tsx` 3/3 + `quantbotIntents.test.ts` 3/3；
全量 vitest 491 passed / 22 failed（**与既有基线同数，零新增**）；typecheck 零错误。

---

## 全局 DoD（每个任务）

1. `npm run typecheck` 通过；组件单测（vitest）或走查清单（视觉类）二者至少其一；
2. 新页面/组件与现有外观并排走查无违和；
3. 完成即勾选本文件 + 一行证据摘要；
4. 提交信息带任务 ID；`web/dist` 构建产物同步按现有部署流程。