/**
 * 推理分折线图 —— 已提升到跨市场共享层（取 A 股版超集实现：含 compact / endDate 能力）。
 * 保留本文件仅为兼容既有 import 路径（inference-center 的 ModelScoreCurveGrid 直接引用本路径）。
 */
export * from '../../stock-terminal-shared/components/InferenceScoreChart';
